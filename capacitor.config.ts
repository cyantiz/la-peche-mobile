import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'la.peche.ionic',
    appName: 'la-peche-ionic',
    webDir: '.output/public',
    server: {
        androidScheme: 'https',
    },
}

export default config
